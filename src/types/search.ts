// Shape produced by tools/extract_search_page.py from search.html.

export interface SearchPageData {
  seo: { title: string | null; description: string | null };
  placeholder: string | null;
  goLabel: string | null;
  heading: string | null;
}
